name: where-to-bake
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.11
  - pip>=24
  - pytorch=2.6
  - cpuonly
  - pyyaml>=5.4
  - pip:
      - transformers>=4.41.0,<5
      - peft>=0.11.0,<0.12
      - accelerate>=0.30.0,<1
      - safetensors>=0.4.3
